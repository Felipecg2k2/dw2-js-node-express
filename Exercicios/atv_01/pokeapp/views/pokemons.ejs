<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo || "Pokémon App" %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container mt-5">
        <h1 class="text-center mb-4">Lista de Pokémons</h1>
        <p class="text-center mb-5">Catálogo de Pokémons com sprites e tipos.</p>

        <div class="card shadow-sm">
            <div class="card-header bg-dark text-white">
                Pokémons Registrados
            </div>
            <div class="card-body p-0">
                <% if (pokemons && pokemons.length > 0) { %>
                    <table class="table table-striped table-hover mb-0">
                        <thead class="thead-dark">
                            <tr>
                                <th scope="col">Número</th>
                                <th scope="col">Nome</th>
                                <th scope="col">Sprite</th>
                                <th scope="col">Tipo</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% pokemons.forEach((p, index) => { %>
                                <tr>
                                    <th scope="row"><%= p.numero %></th>
                                    <td><%= p.nome %></td>
                                    <td>
                                        <img src="/imgs/<%= p.imagem %>" alt="<%= p.nome %>" class="pokemon-sprite">
                                    </td>
                                    <td>
                                        <span class="badge bg-info"><%= p.tipo %></span>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                <% } else { %>
                    <div class="p-3 text-center text-muted">Nenhum Pokémon encontrado no catálogo.</div>
                <% } %>
            </div>
        </div>
        
        <p class="text-center mt-4"><a href="/" class="btn btn-outline-primary">Voltar para a Home</a></p>
    </div>
</body>
</html>